// features/team/components/PlayerCard.tsx
import type { Player } from "../types";

interface PlayerCardProps {
  player: Player;
}

export function PlayerCard({ player: _ }: PlayerCardProps) {
  return <div />;
}
