// features/news/components/ArticleCard.tsx
import type { Article } from "../types";

interface ArticleCardProps {
  article: Article;
}

export function ArticleCard({ article: _ }: ArticleCardProps) {
  return <div />;
}
