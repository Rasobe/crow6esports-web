// features/store/components/ProductCard.tsx
import type { Product } from "../types";

interface ProductCardProps {
  product: Product;
}

export function ProductCard({ product: _ }: ProductCardProps) {
  return <div />;
}
